```diff
--- a/index.html
+++ b/index.html
@@ -345,7 +345,7 @@
         });
     
         // On page load, set UI to default language
-        updateUIForLanguage(document.getElementById('language').value || 'COSYenglish');
+        // updateUIForLanguage(document.getElementById('language').value || 'COSYenglish'); // This call is handled by language-handler.js itself on DOMContentLoaded
 
         // Helper: get selected days (single or range)
         function getSelectedDays() {

```
